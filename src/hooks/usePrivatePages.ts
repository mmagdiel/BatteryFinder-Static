// import type { Path } from "../models";
import { useEffect } from "react";
//import { useCookie } from "react-use";
//import { useNavigate } from "react-router";
//import { COOKIE } from "../models";
// const location = useLocation(); useLocation Privs

const home = "/dashboard";
/*
type UsePrivatePage = () => (
  newValue: string,
  options?: Cookies.CookieAttributes | undefined
) => void;

const usePrivatePage: UsePrivatePage = () => {
  const [token, updateCookie] = useCookie(COOKIE);
  const navigate = useNavigate();

  useEffect(() => {
    if (token) {
      navigate(home);
    }
  }, [token]);
  return updateCookie;
};
export { usePrivatePage };
*/
